# see https://symfony.com/doc/current/reference/configuration/framework.html
framework:
    secret: '%env(APP_SECRET)%'

    # Stateless test tool: no login, no server-side session.
    session: false

    #esi: true
    #fragments: true

when@test:
    framework:
        test: true
        session: false
